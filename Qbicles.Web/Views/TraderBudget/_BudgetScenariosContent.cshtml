@using Qbicles.Models.Trader;
@using Qbicles.Models.Trader.Budgets;
@model List<BudgetGroup>
@{
    var workGroups = (List<WorkGroup>)ViewBag.WorkGroups ?? (new List<WorkGroup>());
    var api = (string)ViewBag.DocRetrievalUrl;
}

    <div class="well custom" style="padding-bottom: 5px;">
        <div class="row">
            <div class="col-xs-12 col-sm-4">
                <div class="form-group">
                    <label for="fiscal year">Search for something</label>
                    <input type="text" name="search" id="budget_scenarios_search_key" onkeyup="budgetScenariosSearch()" class="form-control" placeholder="Search for...">
                </div>
            </div>
            <div class="col-xs-12 col-sm-3 col-lg-2">
                <div class="form-group">
                    <label for="ordering">Order by</label>
                    <select name="ordering" class="form-control select2" onchange="budgetScenariosSearch()" id="budget_scenarios_orderby_filter" style="width: 100%;">
                        <option value="" selected></option>
                        <option value="0">Name A-Z</option>
                        <option value="1">Name Z-A</option>
                        <option value="2">Created new-old</option>
                        <option value="3">Created old-new</option>
                        <option value="4">Status</option>
                    </select>
                </div>
            </div>
            @if (workGroups.Any())
            {
                <div class="col-xs-12 col-sm-5 col-lg-6">
                    <div class="form-group">
                        <label for="">&nbsp;</label><br />
                        <button class="btn btn-success" data-toggle="modal" onclick="budgetScenatioAddEdit(0)" data-target="#app-trader-budgetscenario-add"><i class="fa fa-plus"></i> &nbsp; New Budget Scenario</button>
                    </div>
                </div>
            }
        </div>

    </div>

<br />
<br />


<div class="flex-grid-quarters-lg qbicles-dash-grid theme-labels" id="budget_scenarions_Items">

</div>

<div class="modal fade left" id="app-trader-budgetscenario-add" role="dialog" aria-labelledby="app-trader-budgetscenario-add">

</div><!-- /.modal -->

<script>
    $('#budget_scenarios_orderby_filter').select2({
        placeholder: 'Order by'
    });
    loadBudgetScenariosItem();
</script>
